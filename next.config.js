/** @type {import('next').NextConfig} */
const nextConfig = {
    // trouble shoot for user data
    // async headers() {
    //     return [
    //         {
    //             source: '/:path*',
    //             headers: [{ key: 'referrer-policy', value: 'no-referrer' }],
    //         },
    //     ];
    // },
};

module.exports = nextConfig;
